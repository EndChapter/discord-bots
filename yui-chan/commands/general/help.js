/* const { Command } = require('discord.js-commando');

module.exports = class Help extends Command {
	constructor(client) {
		super(client, {
			name: 'help',
			aliases: ['yardım'],
			group: 'general',
			memberName: 'help',
			description: 'Yardım mesajını görüntüler',
		});
	}
	run(message) {

	}
}; */
